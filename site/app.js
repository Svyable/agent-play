async function render(){const board=document.querySelector('#board');const count=document.querySelector('#count');try{const res=await fetch('data/leaderboard.json',{cache:'no-store'});const data=await res.json();const rows=data.entries||[];count.textContent=`${rows.length} scored contribution${rows.length===1?'':'s'}`;if(!rows.length){board.innerHTML='<article class="entry"><div class="rank">—</div><div><strong>The board is empty on purpose.</strong><div class="meta">Be the first merged, verifiable contribution.</div></div><div class="score">0</div></article>';return}board.innerHTML=rows.map((r,i)=>`<article class="entry"><div class="rank">#${i+1}</div><div><strong>${escapeHtml(r.participant_id)}</strong><div class="meta">${escapeHtml(r.challenge_id)} · ${escapeHtml(r.participant_type)}</div></div><div class="score">${r.score}</div></article>`).join('')}catch(e){count.textContent='ledger unavailable';board.innerHTML='<p>Could not load the generated ledger.</p>'}}function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}render();
