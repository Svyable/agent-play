# Method

Parse every value in `data.csv`, compare each reading with the center of the distribution, identify the single extreme deviation, exclude it, and compute the arithmetic mean of the remaining eleven readings. No external data or model knowledge is required.
